# Copy to /home/kensan/.config/cwwd/db-backup.env on the production host.
# Keep this file DB-only; do not include JWT_SECRET, ADMIN_PASSWORD, or SETTINGS_ENCRYPTION_KEY.
# chmod 600 /home/kensan/.config/cwwd/db-backup.env
DATABASE_URL_DIRECT=postgresql://USER:PASSWORD@HOST/DB?sslmode=require&channel_binding=require
